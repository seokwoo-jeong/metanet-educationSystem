82p시작
분석, 설계
class를 어떻게 만들 것인가
class들이 여러개 있고, 각 class는 관계를 맺는다.
그러면 이 class를 가지고 객체를 이루어야 한다.

class들을 잘 연결해서 이어주는 것이 디자인 패턴

class들이 모이면 jar파일로 압축
그러면 jar파일이 컴포넌트

or mapping시 db교체하면 거의 바로 connect된다.
대신, sql를 건드릴 수 없다.

cbd는 부품을 어떻게 잘 조립 할 것인가 하는 방법론
객체지향은 각각의 부품을 잘 만들기 위한 방법론

was: tomcat, glassfish, websphere ...등이 존재
java
DB

web에서 반응형 때문에 bootstrap많이 사용한다.

jquery
- select할려고 만듬(css에서 유래?)
- 내가 원하는 객체를 손쉽게 꺼낼 수 있음
