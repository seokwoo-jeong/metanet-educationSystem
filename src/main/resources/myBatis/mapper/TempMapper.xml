<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.metanet.educationSystem.mapper.TempMapper">

	<select id="getFileNO" resultType="string">
		select max(fileNO) as fileNO from noticeFile
	</select>

	<select id="getFile" parameterType="string" resultType="NoticeFileVO">
		<![CDATA[
		SELECT
			fileNO,
			noticeNO,
			originFileName,
			round(fileSize/1024,0) as fileSize
		FROM
			noticeFile
		WHERE
			noticeNO = #{noticeNO}
			AND deleteFile = 'N'
		]]>
	</select>

	
	<select id="downloadNoticeFile" parameterType="NoticeFileVO" resultType="NoticeFileVO">
		<![CDATA[
			SELECT
				originFileName,
				storedFilePath,
				fileSize
			FROM
				noticeFile
			WHERE
				noticeNO = #{noticeNO}
				AND
				fileNO = #{fileNO}
				AND
				deleteFile = 'N'
		]]>
	</select>
	
	<insert id="insertNotice" parameterType="NoticeVO" useGeneratedKeys="true" keyProperty="noticeNO">
		INSERT INTO notice(noticeNO) VALUES (#{noticeNO})
	</insert>

	<insert id="insertNoticeFileList" parameterType="NoticeFileVO">
    <![CDATA[
    	INSERT INTO noticeFile
    	(
    	fileNO,
    	noticeNO,
    	originFileName,
    	storedFilePath,
    	fileSize
    	
    		)
    		values
    ]]>
		<foreach collection="list" item="item" separator=",">
			(
			#{item.fileNO},
			#{item.noticeNO},
			#{item.originFileName},
			#{item.storedFilePath},
			#{item.fileSize}
			)
		</foreach>


	</insert>
</mapper>